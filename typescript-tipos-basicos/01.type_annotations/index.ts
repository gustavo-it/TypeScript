// Tipos básicos
let nome: string;
let idade: number;
let adulto: boolean;
let simbolo: symbol;

// arrays
let arrayNumbers: Array<number>;

// Objects
let pessoa: {
    nome: string,
    idade: number,
    adulto?: boolean,
}

// Functions
function soma(x: number, y:number) {
    return x + y;
}
