enum Cores {
    VERMELHO = 100, // 0
    AZUL, // 1
    AMARELO, // 2
}

enum Cores {
    ROXO = 'ROXO', 
    VERDE = 201,
}

console.log(Cores);
console.log(Cores.VERMELHO);
console.log(Cores['ROXO']);
console.log(Cores);